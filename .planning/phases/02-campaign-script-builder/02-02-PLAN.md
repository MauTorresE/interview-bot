---
phase: 02-campaign-script-builder
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/app/(dashboard)/campaigns/page.tsx
  - src/app/(dashboard)/campaigns/[id]/page.tsx
  - src/components/campaigns/campaign-card.tsx
  - src/components/campaigns/campaign-grid.tsx
  - src/components/campaigns/create-campaign-dialog.tsx
  - src/components/campaigns/campaign-tabs.tsx
  - src/components/campaigns/summary-tab.tsx
  - src/components/campaigns/status-badge.tsx
autonomous: true
requirements: [CAMP-02, CAMP-03, CAMP-04]
must_haves:
  truths:
    - "User can see all campaigns in a card grid layout with status badges and progress bars"
    - "User can filter campaigns by status"
    - "User can create a new campaign via dialog and be redirected to campaign detail"
    - "User can view campaign detail with tab navigation (Resumen, Guia, Participantes, Config)"
    - "Summary tab shows progress card, quick info card, and recent activity"
  artifacts:
    - path: "src/app/(dashboard)/campaigns/page.tsx"
      provides: "Campaign list page with server-side data fetching"
      contains: "CampaignGrid"
    - path: "src/app/(dashboard)/campaigns/[id]/page.tsx"
      provides: "Campaign detail page with tab layout"
      contains: "CampaignTabs"
    - path: "src/components/campaigns/campaign-card.tsx"
      provides: "Individual campaign card with status, progress, metadata"
    - path: "src/components/campaigns/create-campaign-dialog.tsx"
      provides: "Campaign creation dialog with name, description, language, duration fields"
  key_links:
    - from: "src/app/(dashboard)/campaigns/page.tsx"
      to: "supabase campaigns table"
      via: "supabase.from('campaigns').select()"
      pattern: "from\\('campaigns'\\)\\.select"
    - from: "src/components/campaigns/create-campaign-dialog.tsx"
      to: "src/app/(dashboard)/campaigns/actions.ts"
      via: "import createCampaign"
      pattern: "import.*createCampaign"
---

<objective>
Build the campaign list page (card grid with filtering) and campaign detail shell (tabs with summary tab).

Purpose: Replace the empty campaigns page with a functional campaign list and detail view. Users can see, create, and navigate campaigns.
Output: Campaign list page, campaign detail page, create dialog, status badges, and all supporting components.
</objective>

<execution_context>
@C:/Users/Waniboko/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Waniboko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-campaign-script-builder/02-CONTEXT.md
@.planning/phases/02-campaign-script-builder/02-UI-SPEC.md
@.planning/phases/02-campaign-script-builder/02-RESEARCH.md
@.planning/phases/02-campaign-script-builder/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/lib/validations/campaign.ts:
```typescript
export const createCampaignSchema = z.object({
  name: z.string().min(2).max(100),
  description: z.string().max(500).optional(),
  language: z.enum(['es-419', 'es-ES']).default('es-419'),
  duration_target_minutes: z.enum(['10', '15', '30']).transform(Number).default('15'),
})
export type CreateCampaignInput = z.infer<typeof createCampaignSchema>
```

From src/lib/constants/campaign.ts:
```typescript
export const CAMPAIGN_STATUSES = ['draft', 'active', 'paused', 'completed', 'archived'] as const
export type CampaignStatus = typeof CAMPAIGN_STATUSES[number]
export const CAMPAIGN_STATUS_LABELS: Record<CampaignStatus, string>
export const LANGUAGES = [{ value: 'es-419', label: '...' }, ...] as const
export const DURATION_OPTIONS = [{ value: '10', label: '10 min' }, ...] as const
```

From src/app/(dashboard)/campaigns/actions.ts:
```typescript
export async function createCampaign(input: {
  name: string; description?: string; language: string; duration_target_minutes: number;
}): Promise<{ success?: boolean; error?: string; data?: { id: string } }>
```

From src/app/(dashboard)/layout.tsx:
```typescript
// Dashboard layout wraps children in SidebarProvider + AppSidebar + <main>
// Pages are server components that fetch data via createClient() (RLS-aware)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components and build campaign list page</name>
  <files>src/app/(dashboard)/campaigns/page.tsx, src/components/campaigns/campaign-card.tsx, src/components/campaigns/campaign-grid.tsx, src/components/campaigns/create-campaign-dialog.tsx, src/components/campaigns/status-badge.tsx</files>
  <read_first>
    - src/app/(dashboard)/campaigns/page.tsx (current empty state page — will be replaced)
    - src/app/(dashboard)/layout.tsx (parent layout pattern, server component data fetching)
    - src/app/(dashboard)/settings/actions.ts (server action invocation pattern)
    - src/components/create-org-dialog.tsx (existing dialog pattern with react-hook-form)
    - src/lib/validations/campaign.ts (createCampaignSchema)
    - src/lib/constants/campaign.ts (CAMPAIGN_STATUSES, CAMPAIGN_STATUS_LABELS, LANGUAGES, DURATION_OPTIONS)
    - .planning/phases/02-campaign-script-builder/02-UI-SPEC.md (Campaign List Page layout contract, Campaign Creation Dialog, status badge colors)
  </read_first>
  <action>
**0. Install required shadcn components:**
```bash
npx shadcn@latest add table tabs textarea badge select switch checkbox progress alert-dialog breadcrumb collapsible toggle-group popover command
```

**1. `src/components/campaigns/status-badge.tsx`** — Reusable status badge component.
- `'use client'` since it uses dynamic className.
- Props: `status: string`, `variant: 'campaign' | 'respondent'` (default 'campaign').
- Uses shadcn Badge component with custom className.
- Campaign status colors per UI-SPEC:
  - `draft`: bg `hsl(0 0% 15%)`, text `#a1a1aa`, border `#262626`
  - `active`: bg `hsl(142 71% 45% / 0.12)`, text `#4ade80`, border `hsl(142 71% 45% / 0.25)`
  - `paused`: bg `hsl(38 92% 50% / 0.12)`, text `#fbbf24`, border `hsl(38 92% 50% / 0.25)`
  - `completed`: bg `hsl(239 84% 67% / 0.12)`, text `#818cf8`, border `hsl(239 84% 67% / 0.25)`
  - `archived`: same as draft
- Respondent status colors per UI-SPEC:
  - `invited`: bg `hsl(0 0% 15%)`, text `#a1a1aa`
  - `in_progress`: bg `hsl(38 92% 50% / 0.12)`, text `#fbbf24`
  - `completed`: bg `hsl(142 71% 45% / 0.12)`, text `#4ade80`
  - `dropped`: bg `hsl(0 84% 60% / 0.12)`, text `#f87171`
- Displays Spanish label from CAMPAIGN_STATUS_LABELS or RESPONDENT_STATUS_LABELS.

**2. `src/components/campaigns/campaign-card.tsx`** — Individual campaign card.
- `'use client'` (uses `useRouter` for navigation on click).
- Props: `campaign: { id: string; name: string; description: string | null; status: string; language: string; duration_target_minutes: number; created_at: string; respondent_count: number; completed_count: number }`.
- Uses shadcn Card component.
- Layout per UI-SPEC: status badge (top-right absolute), campaign name (14px semibold), description (truncated 2 lines with `line-clamp-2`, 14px muted), Progress component with `completed_count / respondent_count * 100` fill + "N/M entrevistas" label, language tag (12px), created date formatted with `date-fns` `format(parseISO(created_at), "d MMM yyyy", { locale: es })`.
- Import `es` locale from `date-fns/locale/es`.
- Card hover: `hover:border-primary/30 transition-colors duration-150`.
- Card click: `onClick={() => router.push(\`/campaigns/${campaign.id}\`)}` with `cursor-pointer`.

**3. `src/components/campaigns/campaign-grid.tsx`** — Grid container with filtering.
- `'use client'` (manages filter state).
- Props: `campaigns: Campaign[]` (same type as campaign-card props).
- State: `statusFilter` (string, default '' = all), `searchQuery` (string).
- Filter row: Select component for status filter (options from CAMPAIGN_STATUSES with Spanish labels, plus "Todos" option), Input for search (filters by name, placeholder "Buscar...").
- Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`.
- Renders filtered campaigns as CampaignCard components.
- When no campaigns match filter: show muted text "No se encontraron campanas."

**4. `src/components/campaigns/create-campaign-dialog.tsx`** — Campaign creation dialog.
- `'use client'`.
- Uses shadcn Dialog, react-hook-form + zodResolver with createCampaignSchema.
- Fields per UI-SPEC: name (Input, required), description (Textarea, 3 rows, optional), two-column row with language (Select, LANGUAGES options) and duration (Select, DURATION_OPTIONS).
- Footer: "Cancelar" ghost button, "Crear campana" accent filled button.
- On submit: calls `createCampaign(data)` from `@/app/(dashboard)/campaigns/actions`. On success: close dialog, `router.push(\`/campaigns/${result.data.id}\`)`, `toast.success('Campana creada')`. On error: `toast.error(result.error)`.
- Submitting state: button shows loading spinner, inputs disabled.

**5. `src/app/(dashboard)/campaigns/page.tsx`** — Replace current empty page.
- Server component (no 'use client').
- Fetches campaigns: `supabase.from('campaigns').select('id, name, description, status, language, duration_target_minutes, created_at, respondents(count)').order('created_at', { ascending: false })`.
- Note: Supabase supports `.select('*, respondents(count)')` for aggregate. Map the result to add `respondent_count` (total) and `completed_count` (filter by status='completed') — if Supabase doesn't support filtered count in a single query, do a second query or use the full count and set `completed_count` to 0 for now (the respondent data will be populated when respondents are added in Plan 03).
- Actually, simpler approach: `supabase.from('campaigns').select('id, name, description, status, language, duration_target_minutes, created_at')` and separately `supabase.from('respondents').select('campaign_id, status')` to compute counts per campaign. Group by campaign_id to get counts.
- Page heading "Campanas" (20px heading, `text-xl font-semibold`).
- Top right: CreateCampaignDialog trigger button ("Crear campana", accent filled).
- Render CampaignGrid with campaigns data.
- Empty state (when zero campaigns): centered, lucide FolderOpen 48px muted, "Sin campanas aun", "Crea tu primera campana de entrevistas para comenzar a recopilar insights.", CreateCampaignDialog trigger as CTA.
- Page padding: `p-8` desktop, `p-4` mobile.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/table.tsx exists (shadcn installed)
    - src/components/ui/tabs.tsx exists (shadcn installed)
    - src/components/ui/badge.tsx exists (shadcn installed)
    - src/components/ui/select.tsx exists (shadcn installed)
    - src/components/ui/progress.tsx exists (shadcn installed)
    - src/components/campaigns/status-badge.tsx contains "CAMPAIGN_STATUS_LABELS"
    - src/components/campaigns/campaign-card.tsx contains "respondent_count"
    - src/components/campaigns/campaign-card.tsx contains "hover:border-primary/30"
    - src/components/campaigns/campaign-grid.tsx contains "grid-cols-1 md:grid-cols-2 lg:grid-cols-3"
    - src/components/campaigns/create-campaign-dialog.tsx contains "createCampaign"
    - src/components/campaigns/create-campaign-dialog.tsx contains "Campana creada"
    - src/app/(dashboard)/campaigns/page.tsx contains "from('campaigns').select"
    - src/app/(dashboard)/campaigns/page.tsx contains "CampaignGrid"
    - src/app/(dashboard)/campaigns/page.tsx contains "Sin campanas aun"
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>Campaign list page renders cards in responsive grid with status badges, progress bars, and filtering. Create dialog opens and submits via server action.</done>
</task>

<task type="auto">
  <name>Task 2: Campaign detail page with tabs and summary tab</name>
  <files>src/app/(dashboard)/campaigns/[id]/page.tsx, src/components/campaigns/campaign-tabs.tsx, src/components/campaigns/summary-tab.tsx</files>
  <read_first>
    - src/app/(dashboard)/layout.tsx (parent layout, server component pattern)
    - src/lib/constants/campaign.ts (status labels, style descriptions)
    - src/components/campaigns/status-badge.tsx (just created — reuse for detail page)
    - .planning/phases/02-campaign-script-builder/02-UI-SPEC.md (Campaign Detail Page layout, Tab: Resumen section)
  </read_first>
  <action>
**1. `src/app/(dashboard)/campaigns/[id]/page.tsx`** — Campaign detail page (server component).
- Fetches campaign by id: `supabase.from('campaigns').select('*').eq('id', params.id).single()`.
- If no campaign found: `notFound()` from `next/navigation`.
- Fetches research brief: `supabase.from('research_briefs').select('*').eq('campaign_id', params.id).single()`.
- Fetches respondents: `supabase.from('respondents').select('*').eq('campaign_id', params.id).order('created_at', { ascending: false })`.
- Layout per UI-SPEC:
  - Breadcrumb: "Campanas" (link to `/campaigns`) > campaign.name (current). Use shadcn Breadcrumb component.
  - Top right: DropdownMenu with actions — "Editar detalles" (no-op for now, handled by config tab), "Archivar campana" (opens AlertDialog).
  - Render CampaignTabs with campaign, brief, and respondents data.
- Page padding: `p-8` (desktop), `p-4` (mobile).

**2. `src/components/campaigns/campaign-tabs.tsx`** — Client component managing tab state.
- `'use client'`.
- Props: `campaign`, `brief`, `respondents` (typed inline or with a Campaign interface).
- Uses shadcn Tabs with 4 TabsTrigger: "Resumen", "Guia de investigacion", "Participantes", "Configuracion".
- Active tab: the default tab is "resumen".
- Tab styling per UI-SPEC: active tab has accent underline (3px bottom border `border-b-[3px] border-primary`), inactive tabs have `text-muted-foreground hover:text-foreground`.
- For now, only SummaryTab is fully implemented. Other tabs render placeholder text:
  - "Guia de investigacion" tab content: "Guia sin completar" empty state (will be replaced in Plan 03)
  - "Participantes" tab content: "Sin participantes aun" empty state (will be replaced in Plan 03)
  - "Configuracion" tab content: "Configuracion" heading (will be replaced in Plan 04)
- Tab content area: `pt-8` (32px below tab bar per UI-SPEC).

**3. `src/components/campaigns/summary-tab.tsx`** — Resumen tab content.
- Props: `campaign`, `respondents` (array).
- Two-column layout on desktop (`grid grid-cols-1 lg:grid-cols-[1fr_0.67fr] gap-6`), single column mobile.
- Left column (60%):
  - **Progress card**: Card with Progress component. Fill = `(completedCount / totalCount) * 100` or 0 if no respondents. Label: "N de M entrevistas" below. StatusBadge for campaign status.
  - **Activity card**: Card with "Actividad reciente" heading. Last 5 respondents sorted by created_at desc. Each row: initials avatar (first letter of name, `rounded-full bg-muted w-8 h-8 flex items-center justify-center text-xs`), name, StatusBadge (respondent variant), timestamp (12px muted, formatted with date-fns `formatDistanceToNow` in Spanish locale). Empty state if no respondents: "Sin actividad aun".
- Right column (40%):
  - **Quick info card**: Card with key-value pairs. Keys (12px semibold label): "Idioma", "Duracion", "Voz", "Estilo". Values (14px regular): campaign.language label from LANGUAGES, `${campaign.duration_target_minutes} min`, voice name from VOICE_PERSONAS matching campaign.voice_id (or "Sin asignar" if null), style label from INTERVIEWER_STYLES matching campaign.interviewer_style. 16px vertical gap between pairs.
- Top of tab: campaign status badge + "Creada: " + formatted creation date.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(dashboard)/campaigns/[id]/page.tsx contains "from('campaigns').select"
    - src/app/(dashboard)/campaigns/[id]/page.tsx contains "notFound()"
    - src/app/(dashboard)/campaigns/[id]/page.tsx contains "Breadcrumb"
    - src/components/campaigns/campaign-tabs.tsx contains "TabsTrigger"
    - src/components/campaigns/campaign-tabs.tsx contains "Resumen"
    - src/components/campaigns/campaign-tabs.tsx contains "Guia de investigacion"
    - src/components/campaigns/campaign-tabs.tsx contains "Participantes"
    - src/components/campaigns/campaign-tabs.tsx contains "Configuracion"
    - src/components/campaigns/summary-tab.tsx contains "Progress"
    - src/components/campaigns/summary-tab.tsx contains "Actividad reciente"
    - src/components/campaigns/summary-tab.tsx contains "VOICE_PERSONAS"
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>Campaign detail page loads from DB with breadcrumb navigation and 4-tab layout. Summary tab displays progress, activity, and quick info cards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Server Component | Campaign ID from URL params used in DB query |
| Supabase RLS | Authenticated user can only see campaigns in their org |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Campaign detail page params.id | mitigate | RLS policy ensures `supabase.from('campaigns').select().eq('id', params.id)` only returns campaigns where `org_id = get_org_id()` from JWT; non-matching IDs return null -> notFound() |
| T-02-05 | Information Disclosure | Campaign list query | mitigate | Server component uses authenticated Supabase client which applies RLS automatically; no admin client used for reads |
</threat_model>

<verification>
- `npm run build` — no TypeScript errors, all routes compile
- Campaign list page loads with grid layout
- Campaign detail page loads with tab navigation
- Summary tab displays progress and quick info
</verification>

<success_criteria>
- Campaign list page replaces empty state with card grid, status filter, search, and create dialog
- Campaign detail page loads by ID with breadcrumb navigation
- 4 tabs render (Resumen fully implemented, others with placeholder)
- Summary tab shows progress bar, activity feed, and quick info card
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-campaign-script-builder/02-02-SUMMARY.md`
</output>
