---
phase: 03-voice-interview
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/app/interview/[token]/lobby-screen.tsx
  - src/app/interview/[token]/interview-room.tsx
  - src/app/interview/[token]/interview-flow-wrapper.tsx
  - src/components/interview/interview-orb.tsx
  - src/components/interview/mic-level-meter.tsx
  - src/components/interview/transcript-feed.tsx
  - src/components/interview/text-fallback-input.tsx
  - src/components/interview/interview-timer.tsx
  - src/components/interview/phase-indicator.tsx
autonomous: true
requirements: [WEBR-01, WEBR-05, WEBR-07, DASH-05]

must_haves:
  truths:
    - "Respondent sees lobby with mic check, device selector, and campaign info after consent (D-27)"
    - "Lobby disables Comenzar button until mic audio is detected (D-27)"
    - "Interview room shows morphing violet orb with 4 visual states (D-29)"
    - "Real-time transcript with speaker labels scrolls during interview (D-02)"
    - "Text fallback input is always visible at bottom (D-03)"
    - "Mic mute toggle and elapsed timer are visible in bottom controls (DASH-05)"
    - "Phase indicator shows current conversation phase below orb (D-32)"
    - "Page transitions use 300ms opacity fade (D-28)"
  artifacts:
    - path: "src/app/interview/[token]/lobby-screen.tsx"
      provides: "Pre-interview mic check screen"
      contains: "useMediaDeviceSelect"
    - path: "src/app/interview/[token]/interview-room.tsx"
      provides: "Main interview room orchestrator"
      contains: "LiveKitRoom"
    - path: "src/components/interview/interview-orb.tsx"
      provides: "Morphing violet orb with 4 states"
      contains: "useAgent"
    - path: "src/components/interview/transcript-feed.tsx"
      provides: "Scrolling transcript with speaker labels"
      contains: "role=\"log\""
  key_links:
    - from: "src/app/interview/[token]/interview-flow-wrapper.tsx"
      to: "src/app/interview/[token]/lobby-screen.tsx"
      via: "phase === 'lobby' renders LobbyScreen"
      pattern: "LobbyScreen"
    - from: "src/app/interview/[token]/interview-room.tsx"
      to: "src/components/interview/interview-orb.tsx"
      via: "Composes orb component"
      pattern: "InterviewOrb"
    - from: "src/components/interview/interview-orb.tsx"
      to: "LiveKit SDK"
      via: "useAgent() for state, useTrackVolume() for amplitude"
      pattern: "useAgent"
---

<objective>
Lobby screen, interview room, and all interview UI components.

Purpose: This is the core respondent-facing experience -- the lobby where they check their mic, and the interview room where they talk to the AI. The morphing violet orb, real-time transcript, text fallback, and phase indicator create the premium UX described in the CONTEXT.md decisions.

Output: Lobby screen component, interview room component, and 6 reusable interview UI components. InterviewFlowWrapper updated to render all phases.
</objective>

<execution_context>
@C:\Users\Waniboko\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Waniboko\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-voice-interview/03-CONTEXT.md
@.planning/phases/03-voice-interview/03-RESEARCH.md
@.planning/phases/03-voice-interview/03-UI-SPEC.md

<interfaces>
<!-- From Plan 01 (must exist before this plan runs) -->

From src/app/interview/[token]/interview-flow-wrapper.tsx:
```typescript
type FlowPhase = 'consent' | 'lobby' | 'interview' | 'completion'
type InterviewSession = { token: string; wsUrl: string; interviewId: string }
// Manages phase state, renders consent/lobby/interview/completion
// Placeholder divs for lobby, interview, completion -- replace with real components
```

From src/app/api/livekit/token/route.ts:
```typescript
// POST response: { token: string, roomName: string, interviewId: string, wsUrl: string }
```

<!-- LiveKit React SDK hooks (from RESEARCH.md, verified) -->
From @livekit/components-react:
```typescript
useAgent() -> { state: 'disconnected'|'connecting'|'listening'|'thinking'|'speaking', audioTrack: TrackReference }
useTrackVolume(trackRef) -> number (0-1)
useConnectionState() -> ConnectionState
useDataChannel(topic?) -> { send: (data: Uint8Array, options?) => void, message: DataChannelMessage }
useIsSpeaking(participant) -> boolean
useLocalParticipant() -> { localParticipant: LocalParticipant, ... }
useMediaDeviceSelect({ kind: 'audioinput' }) -> { devices: MediaDeviceInfo[], activeDeviceId: string, setActiveMediaDevice: (id: string) => Promise<void> }
useIsMuted(trackRef) -> boolean
LiveKitRoom -> Provider component, props: serverUrl, token, connect, audio, onDisconnected, onConnected
```

<!-- UI-SPEC color/spacing values -->
Orb colors:
- Base: #6366F1 (hsl(239 84% 67%))
- Idle glow: 0 0 40px rgba(99,102,241,0.3)
- Listening glow: 0 0 60px rgba(99,102,241,0.5)
- Thinking glow: 0 0 30px rgba(99,102,241,0.2), opacity 0.85
- Speaking glow: 0 0 80px rgba(99,102,241,0.6)

Layout:
- Full viewport 100dvh, content centered max-w-[640px]
- Orb: 120px desktop, 96px mobile
- Bottom controls: 48px height
- Text input: 48px height, --card background
- Touch targets: minimum 44px
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lobby screen and interview UI components</name>
  <files>src/app/interview/[token]/lobby-screen.tsx, src/components/interview/interview-orb.tsx, src/components/interview/mic-level-meter.tsx, src/components/interview/transcript-feed.tsx, src/components/interview/text-fallback-input.tsx, src/components/interview/interview-timer.tsx, src/components/interview/phase-indicator.tsx</files>
  <read_first>
    - src/app/interview/[token]/interview-flow-wrapper.tsx (parent component, need to understand props interface)
    - src/components/ui/button.tsx (button component API)
    - src/components/ui/card.tsx (card component API)
    - src/components/ui/select.tsx (select component API for mic selector)
    - src/components/ui/scroll-area.tsx (scroll area for transcript)
    - .planning/phases/03-voice-interview/03-UI-SPEC.md (full visual specifications)
  </read_first>
  <action>
1. Create `src/components/interview/mic-level-meter.tsx`:
   - Props: `{ volume: number }` (0-1 from useTrackVolume)
   - 8px tall horizontal bar. Background: `border` color. Fill: `#22c55e` (success green) with width proportional to volume
   - Below bar: "Audio detectado" in green when volume > 0.01, "No se detecta audio. Verifica tu microfono." in destructive when volume <= 0.01
   - Fill transitions: `transition-[width]` with `duration-[50ms]`
   - Export: `MicLevelMeter`

2. Create `src/components/interview/interview-timer.tsx`:
   - Props: `{ elapsedSeconds: number; targetSeconds: number }`
   - Displays "MM:SS / MM:SS" format using Geist Mono font (`font-mono`)
   - Text color: `text-muted-foreground`
   - Format function: pad minutes and seconds with leading zeros
   - Export: `InterviewTimer`

3. Create `src/components/interview/phase-indicator.tsx`:
   - Props: `{ phase: 'warmup' | 'conversation' | 'closing' }`
   - Maps phase to Spanish label: warmup->"Calentamiento", conversation->"Conversacion", closing->"Cierre"
   - 12px semibold, `text-primary` color
   - Fade transition on phase change: 200ms opacity ease-in-out (use key={phase} for React re-render with CSS transition)
   - `aria-live="polite"` for screen reader announcements
   - Export: `PhaseIndicator`

4. Create `src/components/interview/transcript-feed.tsx`:
   - Props: `{ entries: Array<{ id: string; speaker: 'bot' | 'client'; content: string; elapsedMs: number }> }`
   - Uses shadcn ScrollArea for container
   - Each entry: speaker label (12px semibold, "Entrevistador" for bot in `text-primary`, "Participante" for client in `text-foreground`), timestamp (12px Geist Mono, `text-muted-foreground`), message text (14px regular)
   - 16px vertical gap between entries
   - Auto-scroll to bottom on new entry using `useEffect` with `scrollIntoView({ behavior: 'smooth' })`
   - Detects manual scroll-up (scroll position > 100px from bottom) and pauses auto-scroll
   - Container: `role="log"` and `aria-live="polite"`
   - Empty state: "La transcripcion aparecera aqui cuando comience la entrevista." in muted text
   - Export: `TranscriptFeed`

5. Create `src/components/interview/text-fallback-input.tsx`:
   - Props: `{ onSend: (text: string) => void; disabled?: boolean }`
   - 48px height input bar with `--card` background, `--border` border
   - Placeholder: "Escribe tu respuesta..."
   - Send button: lucide `SendHorizontal` icon, 20px, `text-primary`, inside input right side
   - Enter key submits, clears input
   - Disabled state: opacity 0.5 (during agent speaking)
   - Focus ring: `--primary` color
   - `aria-label="Escribe tu respuesta"`
   - Export: `TextFallbackInput`

6. Create `src/components/interview/interview-orb.tsx`:
   - Props: `{ state: 'idle' | 'listening' | 'thinking' | 'speaking'; volume: number }`
   - 120px circle desktop, 96px mobile (`w-[120px] h-[120px] md:w-[120px]` -- use responsive classes)
   - Base color: `bg-primary` (#6366F1)
   - CSS animations defined inline or via Tailwind arbitrary values:
     - Idle: scale pulse 1.0->1.03, 2s ease-in-out infinite. Glow: `shadow-[0_0_40px_rgba(99,102,241,0.3)]`
     - Listening: scale breathe 1.0->1.05, 3s ease-in-out infinite. Glow: `shadow-[0_0_60px_rgba(99,102,241,0.5)]`. Ripple ring (pseudo-element or sibling div animating scale 1->2 with opacity fade, 1.5s interval)
     - Thinking: scale(0.95), opacity 0.85, shimmer via rotating radial-gradient (400ms cycle). Glow: `shadow-[0_0_30px_rgba(99,102,241,0.2)]`
     - Speaking: border-radius morphing driven by volume prop. Volume 0-1 maps to border-radius variance: `50%` base +/- `15%` on each corner. Use inline style: `borderRadius: calculateBlobRadius(volume)`. Glow: `shadow-[0_0_80px_rgba(99,102,241,0.6)]`
   - State transitions: 250-300ms ease-out on transform, box-shadow, opacity, border-radius
   - Below orb: state label (12px, muted-foreground): "Escuchando..." for listening, "Procesando..." for thinking. No label for idle or speaking.
   - Define CSS keyframes in a `<style>` tag or use Tailwind `animate-` with custom keyframes in tailwind config
   - Export: `InterviewOrb`

7. Create `src/app/interview/[token]/lobby-screen.tsx` (D-27):
   - Props: `{ session: InterviewSession; campaignName: string; onStart: () => void }`
   - Uses `LiveKitRoom` provider with `connect={true}` and `audio={true}` from `@livekit/components-react`
   - Inside LiveKitRoom, a `LobbyContent` component uses:
     - `useMediaDeviceSelect({ kind: 'audioinput' })` for mic selector -> renders shadcn Select with device list
     - `useTrackVolume()` on local audio track -> feeds MicLevelMeter
     - `useLocalParticipant()` to get local participant track ref
   - Card layout (max-w-[480px] centered, `--card` background, 32px padding, rounded-xl):
     - Campaign name (20px semibold, centered)
     - "Entrevista de {duration} minutos" (14px muted, centered)
     - Mic device selector (shadcn Select)
     - MicLevelMeter component
     - "Entrevistador: {persona_name}" (14px regular)
     - "Duracion estimada: {duration} min" (14px regular)
     - "Comenzar entrevista" button (accent filled, full width, disabled until mic level > 0.01)
   - Note: The lobby connects to LiveKit room but the interview doesn't start until the respondent clicks Comenzar. The agent will join when it detects a participant.
   - The lobby needs campaign info (duration, persona name). This can be fetched via a separate API call or passed from the token route response. For simplicity, add `campaignInfo: { duration: number; personaName: string }` to the InterviewSession type.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/interview/mic-level-meter.tsx contains "MicLevelMeter"
    - src/components/interview/interview-timer.tsx contains "InterviewTimer" and "font-mono"
    - src/components/interview/phase-indicator.tsx contains "PhaseIndicator" and "aria-live"
    - src/components/interview/transcript-feed.tsx contains "TranscriptFeed" and 'role="log"'
    - src/components/interview/transcript-feed.tsx contains "scrollIntoView"
    - src/components/interview/transcript-feed.tsx contains "Entrevistador" and "Participante"
    - src/components/interview/text-fallback-input.tsx contains "TextFallbackInput" and "SendHorizontal"
    - src/components/interview/text-fallback-input.tsx contains "Escribe tu respuesta"
    - src/components/interview/interview-orb.tsx contains "InterviewOrb"
    - src/components/interview/interview-orb.tsx contains "idle" and "listening" and "thinking" and "speaking"
    - src/components/interview/interview-orb.tsx contains "rgba(99,102,241"
    - src/app/interview/[token]/lobby-screen.tsx contains "useMediaDeviceSelect"
    - src/app/interview/[token]/lobby-screen.tsx contains "MicLevelMeter"
    - src/app/interview/[token]/lobby-screen.tsx contains "Comenzar entrevista"
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Lobby screen with mic check and device selector. Six interview UI components: orb with 4 animation states, transcript feed with auto-scroll, text fallback input, timer, phase indicator, mic level meter. All compile clean.</done>
</task>

<task type="auto">
  <name>Task 2: Interview room orchestrator and flow wrapper integration</name>
  <files>src/app/interview/[token]/interview-room.tsx, src/app/interview/[token]/interview-flow-wrapper.tsx</files>
  <read_first>
    - src/app/interview/[token]/interview-flow-wrapper.tsx (update placeholders with real components)
    - src/app/interview/[token]/lobby-screen.tsx (just created, understand props)
    - src/components/interview/interview-orb.tsx (just created)
    - src/components/interview/transcript-feed.tsx (just created)
    - src/components/interview/text-fallback-input.tsx (just created)
    - src/components/interview/interview-timer.tsx (just created)
    - src/components/interview/phase-indicator.tsx (just created)
    - .planning/phases/03-voice-interview/03-UI-SPEC.md (layout contract for interview room)
  </read_first>
  <action>
1. Create `src/app/interview/[token]/interview-room.tsx` (D-01, D-24, DASH-05):

   ```typescript
   'use client'
   // Main interview room orchestrator
   // Wraps content in LiveKitRoom provider (if not already wrapped by lobby)
   // Composes: InterviewOrb, PhaseIndicator, TranscriptFeed, TextFallbackInput, mic toggle, InterviewTimer
   ```

   Props: `{ session: InterviewSession; onInterviewEnd: (data: { duration: number; topicsCount: number }) => void }`

   Structure:
   - Full viewport height: `h-dvh flex flex-col`
   - Content centered: `max-w-[640px] mx-auto w-full`
   - Padding: `px-8 md:px-8` (32px), `px-4` on mobile (16px)

   Inside `LiveKitRoom` provider (serverUrl={session.wsUrl}, token={session.token}, connect={true}, audio={true}):

   Create `InterviewRoomContent` component that uses LiveKit hooks:
   - `useAgent()` -> get agent state and audioTrack
   - `useTrackVolume(agentAudioTrack)` -> volume for orb speaking state
   - `useDataChannel('text-input')` -> { send } for text fallback
   - `useDataChannel()` -> { message } for receiving phase_change, interview_ended, transcript entries from agent
   - `useLocalParticipant()` -> for mic mute toggle
   - `useConnectionState()` -> for connection state handling
   - `useIsMuted()` -> for mic button state

   State management:
   - `phase: 'warmup' | 'conversation' | 'closing'` -- updated when agent sends phase_change via data channel
   - `entries: TranscriptEntry[]` -- updated when agent sends transcript data via data channel, or via Supabase polling as fallback
   - `elapsedSeconds: number` -- incremented every second via setInterval
   - `targetSeconds: number` -- from session.campaignInfo.duration * 60

   Layout (top to bottom):
   ```
   [fixed top region ~200px]
     InterviewOrb (state from useAgent, volume from useTrackVolume)
     Orb state label
     PhaseIndicator (phase from state)
   [flex-1 region]
     TranscriptFeed (entries from state)
   [fixed bottom bar 48px + padding]
     TextFallbackInput (sends via data channel 'text-input')
     Mic toggle button (44x44, lucide Mic/MicOff)
     InterviewTimer (elapsedSeconds, targetSeconds)
   ```

   Data channel message handling:
   - Listen for messages. Parse JSON.
   - `{"type": "phase_change", "phase": "..."}` -> update phase state
   - `{"type": "interview_ended", "duration": ..., "topics_count": ...}` -> call onInterviewEnd callback
   - `{"type": "transcript", "speaker": "...", "content": "...", "elapsed_ms": ...}` -> add to entries array

   Text input handler:
   - `handleSendText(text: string)` -> encode as JSON `{"type": "text_input", "text": text}` and send via data channel
   - Disable text input when agent is speaking (useAgent state === 'speaking')

   Mic toggle:
   - Toggle local participant audio track enabled/disabled
   - lucide Mic icon when unmuted (foreground color, card background)
   - lucide MicOff icon when muted (destructive color, destructive 10% background)

   End interview button (D-04):
   - "Terminar entrevista" text, destructive text color, no fill
   - On click: show AlertDialog confirmation with copy from UI-SPEC
   - On confirm: send `{"type": "end_interview"}` via data channel

   Connection state handling (D-11):
   - Reconnecting: show sonner toast "Se perdio la conexion. Intentando reconectar..." with amber
   - Disconnected: show persistent toast "No se pudo reconectar. La entrevista fue guardada hasta este punto."
   - On disconnect, mark interview as dropped (or let agent handle it)

2. Update `src/app/interview/[token]/interview-flow-wrapper.tsx`:
   - Import LobbyScreen, InterviewRoom (CompletionCard will be added in Plan 04)
   - Replace lobby placeholder with `<LobbyScreen session={session} campaignName={campaignName} onStart={() => setPhase('interview')} />`
   - Replace interview placeholder with `<InterviewRoom session={session} onInterviewEnd={handleInterviewEnd} />`
   - Add `handleInterviewEnd(data)` that stores duration/topicsCount in state and sets phase to 'completion'
   - Implement 300ms fade transitions between phases (D-28):
     - Use CSS classes with `opacity-0` / `opacity-100` and `transition-opacity duration-300 ease-out`
     - AnimatePresence-style: fade out current phase, then fade in next
     - Simple approach: use `key={phase}` on wrapper div with fade-in animation
   - Completion phase still a placeholder (Plan 04 implements CompletionCard)
   - Add the `campaignInfo` data (duration, persona name) to the token route response and pass through. Alternatively, fetch from a new endpoint. The simplest approach: extend the token route response to include `campaignInfo: { duration: number, personaName: string }` and store it in the session state.

   Update the `InterviewSession` type to:
   ```typescript
   type InterviewSession = {
     token: string
     wsUrl: string
     interviewId: string
     campaignInfo: {
       duration: number      // minutes
       personaName: string
     }
   }
   ```
   This requires also updating the token route (Plan 01's route.ts) to include campaignInfo in the response. If Plan 01 is already executed, add a small update to the token route to include this data by looking up the campaign's duration_target and voice_persona name.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/app/interview/[token]/interview-room.tsx contains "InterviewRoom" or "InterviewRoomContent"
    - src/app/interview/[token]/interview-room.tsx contains "LiveKitRoom"
    - src/app/interview/[token]/interview-room.tsx contains "useAgent"
    - src/app/interview/[token]/interview-room.tsx contains "useTrackVolume"
    - src/app/interview/[token]/interview-room.tsx contains "useDataChannel"
    - src/app/interview/[token]/interview-room.tsx contains "InterviewOrb"
    - src/app/interview/[token]/interview-room.tsx contains "TranscriptFeed"
    - src/app/interview/[token]/interview-room.tsx contains "TextFallbackInput"
    - src/app/interview/[token]/interview-room.tsx contains "InterviewTimer"
    - src/app/interview/[token]/interview-room.tsx contains "Terminar entrevista"
    - src/app/interview/[token]/interview-room.tsx contains "h-dvh" or "100dvh"
    - src/app/interview/[token]/interview-room.tsx contains "text_input" (data channel send)
    - src/app/interview/[token]/interview-room.tsx contains "phase_change" (data channel receive)
    - src/app/interview/[token]/interview-room.tsx contains "interview_ended" (data channel receive)
    - src/app/interview/[token]/interview-flow-wrapper.tsx contains "LobbyScreen"
    - src/app/interview/[token]/interview-flow-wrapper.tsx contains "InterviewRoom"
    - src/app/interview/[token]/interview-flow-wrapper.tsx contains "duration-300"
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Interview room orchestrates orb, transcript, text input, mic toggle, timer, and phase indicator in a full-viewport layout. Data channel handles text input sending, phase changes, and interview end signals. Flow wrapper renders lobby and interview room with 300ms fade transitions. TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LiveKit Cloud -> Browser | WebRTC media and data channel messages arrive from server |
| Browser -> LiveKit Cloud | User audio and text input sent to agent via WebRTC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Spoofing | LiveKitRoom connection | mitigate | Room connection uses JWT token from server-side /api/livekit/token. Token has room-specific grant. Cannot join other rooms. |
| T-03-14 | Information Disclosure | Transcript data channel | accept | Transcript entries sent via data channel are visible to the participant (who is the speaker). No cross-session leakage -- data channels are room-scoped. |
| T-03-15 | Tampering | Text input via data channel | mitigate | Text sent via data channel is processed by Claude LLM on agent side. No direct DB injection. Agent validates JSON parse. |
</threat_model>

<verification>
- Lobby screen renders mic check with device selector
- Interview room renders orb, transcript, text input, timer, controls
- Flow wrapper transitions between all phases
- `npx tsc --noEmit` passes
- `npx vitest run` passes
</verification>

<success_criteria>
Complete respondent-facing interview experience: lobby with mic check -> interview room with morphing orb, live transcript, text fallback, mic controls, timer, and phase indicator. All transitions use 300ms fades. Layout is full-viewport, centered, responsive (120px orb desktop, 96px mobile).
</success_criteria>

<output>
After completion, create `.planning/phases/03-voice-interview/03-03-SUMMARY.md`
</output>
