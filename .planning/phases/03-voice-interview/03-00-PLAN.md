---
phase: 03-voice-interview
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/api/livekit-token.test.ts
  - tests/components/transcript-feed.test.tsx
  - tests/components/text-fallback-input.test.tsx
  - tests/components/interview-room.test.tsx
  - agent/tests/conftest.py
  - agent/tests/test_interview_state.py
  - agent/pytest.ini
autonomous: true
requirements: [WEBR-01, WEBR-05, WEBR-07, WEBR-08, DASH-05]

must_haves:
  truths:
    - "Test stubs exist for token route, transcript feed, text fallback input, and interview room"
    - "Python test infrastructure exists with pytest.ini and conftest.py with mock fixtures"
    - "InterviewState guardrail tests verify 80% nudge and 95% force-close thresholds"
    - "All test files can be discovered and run (even if tests initially fail as stubs)"
  artifacts:
    - path: "tests/api/livekit-token.test.ts"
      provides: "Token route test stubs covering WEBR-01"
      contains: "describe"
    - path: "tests/components/transcript-feed.test.tsx"
      provides: "Transcript feed test stubs covering WEBR-05"
      contains: "describe"
    - path: "tests/components/text-fallback-input.test.tsx"
      provides: "Text fallback input test stubs covering WEBR-07"
      contains: "describe"
    - path: "tests/components/interview-room.test.tsx"
      provides: "Interview room test stubs covering DASH-05"
      contains: "describe"
    - path: "agent/tests/test_interview_state.py"
      provides: "InterviewState guardrail tests covering WEBR-08"
      contains: "test_should_nudge"
    - path: "agent/pytest.ini"
      provides: "Python test configuration"
      contains: "testpaths"
    - path: "agent/tests/conftest.py"
      provides: "Shared pytest fixtures with mock Supabase client"
      contains: "fixture"
  key_links: []
---

<objective>
Wave 0 test scaffold for Phase 3 -- create test stubs and infrastructure before implementation begins.

Purpose: Ensures all verify commands in Plans 01-04 have actual test files to run against. Without these, verify commands produce zero signal on new code. Also establishes Python test infrastructure for the agent directory.

Output: Test stub files for frontend (Vitest) and agent (pytest), ready to be filled with assertions as implementation proceeds.
</objective>

<execution_context>
@C:\Users\Waniboko\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Waniboko\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-voice-interview/03-RESEARCH.md

<interfaces>
From vitest.config.ts:
```typescript
// Existing Vitest config -- tests discovered from tests/ directory
// jsdom environment for component tests
```

From agent/ directory (created by Plan 02):
```
agent/
  entrevista_agent.py
  interview_state.py
  interview_prompts.py
  supabase_client.py
  voxtral_tts.py
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend test stubs (Vitest)</name>
  <files>tests/api/livekit-token.test.ts, tests/components/transcript-feed.test.tsx, tests/components/text-fallback-input.test.tsx, tests/components/interview-room.test.tsx</files>
  <read_first>
    - vitest.config.ts (test configuration, environment settings)
    - tests/ (list existing test files to follow patterns)
  </read_first>
  <action>
1. Create `tests/api/livekit-token.test.ts` (covers WEBR-01):
   ```typescript
   import { describe, it, expect, vi } from 'vitest'

   // Mock Supabase admin client
   vi.mock('@/lib/supabase/admin', () => ({
     createAdminClient: vi.fn(),
   }))

   // Mock LiveKit server SDK
   vi.mock('livekit-server-sdk', () => ({
     AccessToken: vi.fn(),
     RoomServiceClient: vi.fn(),
     EgressClient: vi.fn(),
     EncodedFileOutput: vi.fn(),
     S3Upload: vi.fn(),
   }))

   describe('/api/livekit/token', () => {
     it('rejects requests with missing token', async () => {
       // Stub: test POST with empty body returns 400
       expect(true).toBe(true) // placeholder
     })

     it('rejects invalid invite tokens', async () => {
       // Stub: test POST with non-existent token returns 400
       expect(true).toBe(true)
     })

     it('rejects respondents without consent (status !== in_progress)', async () => {
       // Stub: test POST with respondent status 'pending' returns 400
       expect(true).toBe(true)
     })

     it('rejects duplicate active interviews', async () => {
       // Stub: test POST when active interview exists returns 409
       expect(true).toBe(true)
     })

     it('creates interview and returns LiveKit token with campaignInfo', async () => {
       // Stub: test POST with valid token creates interview row,
       // returns { token, roomName, interviewId, wsUrl, campaignInfo }
       expect(true).toBe(true)
     })

     it('includes campaignInfo with duration and personaName in response', async () => {
       // Stub: verify response.campaignInfo contains duration (number) and personaName (string)
       expect(true).toBe(true)
     })
   })
   ```

2. Create `tests/components/transcript-feed.test.tsx` (covers WEBR-05):
   ```tsx
   import { describe, it, expect } from 'vitest'
   import { render, screen } from '@testing-library/react'

   describe('TranscriptFeed', () => {
     it('renders empty state message when no entries', () => {
       // Stub: render with empty entries array, expect empty state text
       expect(true).toBe(true)
     })

     it('renders entries with correct speaker labels', () => {
       // Stub: render with bot and client entries, expect "Entrevistador" and "Participante"
       expect(true).toBe(true)
     })

     it('has role="log" and aria-live="polite" for accessibility', () => {
       // Stub: verify ARIA attributes
       expect(true).toBe(true)
     })

     it('auto-scrolls to bottom on new entry', () => {
       // Stub: verify scrollIntoView is called
       expect(true).toBe(true)
     })
   })
   ```

3. Create `tests/components/text-fallback-input.test.tsx` (covers WEBR-07):
   ```tsx
   import { describe, it, expect, vi } from 'vitest'
   import { render, screen, fireEvent } from '@testing-library/react'

   describe('TextFallbackInput', () => {
     it('calls onSend with text when Enter is pressed', () => {
       // Stub: render, type text, press Enter, verify onSend called
       expect(true).toBe(true)
     })

     it('clears input after sending', () => {
       // Stub: verify input value is empty after submit
       expect(true).toBe(true)
     })

     it('does not send empty text', () => {
       // Stub: press Enter with empty input, verify onSend NOT called
       expect(true).toBe(true)
     })

     it('is disabled when disabled prop is true', () => {
       // Stub: render with disabled=true, verify input is disabled
       expect(true).toBe(true)
     })
   })
   ```

4. Create `tests/components/interview-room.test.tsx` (covers DASH-05):
   ```tsx
   import { describe, it, expect, vi } from 'vitest'

   // Mock LiveKit components-react
   vi.mock('@livekit/components-react', () => ({
     LiveKitRoom: vi.fn(({ children }) => children),
     useAgent: vi.fn(() => ({ state: 'listening', audioTrack: null })),
     useTrackVolume: vi.fn(() => 0),
     useDataChannel: vi.fn(() => ({ send: vi.fn(), message: null })),
     useLocalParticipant: vi.fn(() => ({ localParticipant: null })),
     useConnectionState: vi.fn(() => 'connected'),
     useIsMuted: vi.fn(() => false),
   }))

   describe('InterviewRoom', () => {
     it('renders InterviewOrb component', () => {
       // Stub: verify orb is rendered
       expect(true).toBe(true)
     })

     it('renders TranscriptFeed component', () => {
       // Stub: verify transcript feed is rendered
       expect(true).toBe(true)
     })

     it('renders TextFallbackInput component', () => {
       // Stub: verify text input is rendered
       expect(true).toBe(true)
     })

     it('renders InterviewTimer component', () => {
       // Stub: verify timer is rendered
       expect(true).toBe(true)
     })

     it('renders mic toggle button', () => {
       // Stub: verify mic toggle exists
       expect(true).toBe(true)
     })

     it('renders "Terminar entrevista" button', () => {
       // Stub: verify end interview button exists
       expect(true).toBe(true)
     })
   })
   ```
  </action>
  <verify>
    <automated>npx vitest run tests/api/livekit-token.test.ts tests/components/transcript-feed.test.tsx tests/components/text-fallback-input.test.tsx tests/components/interview-room.test.tsx --reporter=verbose</automated>
  </verify>
  <done>All 4 frontend test stub files exist and are discoverable by Vitest. Stub tests pass (placeholder assertions). Ready to be filled with real assertions as Plans 01-04 are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Python test infrastructure and InterviewState tests</name>
  <files>agent/pytest.ini, agent/tests/conftest.py, agent/tests/test_interview_state.py</files>
  <read_first>
    - agent/interview_state.py (if already created by Plan 02 Task 1, read for test targets)
  </read_first>
  <action>
1. Create `agent/pytest.ini`:
   ```ini
   [pytest]
   testpaths = tests
   python_files = test_*.py
   python_classes = Test*
   python_functions = test_*
   asyncio_mode = auto
   ```

2. Create `agent/tests/__init__.py` (empty file for package discovery).

3. Create `agent/tests/conftest.py`:
   ```python
   import pytest
   from unittest.mock import AsyncMock, MagicMock, patch

   @pytest.fixture
   def mock_supabase():
       """Mock Supabase client for agent tests."""
       client = MagicMock()
       client.table = MagicMock(return_value=client)
       client.from_ = MagicMock(return_value=client)
       client.select = MagicMock(return_value=client)
       client.insert = MagicMock(return_value=client)
       client.update = MagicMock(return_value=client)
       client.eq = MagicMock(return_value=client)
       client.single = MagicMock(return_value=client)
       client.execute = AsyncMock(return_value=MagicMock(data=[]))
       return client

   @pytest.fixture
   def mock_interview_config():
       """Mock InterviewConfig for agent tests."""
       config = MagicMock()
       config.research_brief = MagicMock(
           goals="Test goals",
           data_points="Test data points",
           context="Test context",
           tone="Test tone",
       )
       config.voice_persona = "voxtral-natalia"
       config.interviewer_style = "professional"
       config.duration_target_seconds = 900
       config.duration_target_minutes = 15
       config.respondent_name = "Test Respondent"
       return config
   ```

4. Create `agent/tests/test_interview_state.py` (covers WEBR-08):
   ```python
   import time
   import pytest
   from interview_state import InterviewState

   class TestInterviewState:
       def test_initial_state(self):
           state = InterviewState(900)  # 15 min
           assert state.phase == 'warmup'
           assert state.ended == False
           assert state.topics_count == 0

       def test_elapsed_seconds(self):
           state = InterviewState(900)
           # Should be close to 0 at creation
           assert state.elapsed_seconds < 2

       def test_elapsed_fraction(self):
           state = InterviewState(900)
           state.started_at = time.time() - 450  # 7.5 min elapsed
           assert 0.49 < state.elapsed_fraction < 0.51

       def test_time_context_format(self):
           state = InterviewState(900)
           ctx = state.time_context
           assert "minutos" in ctx.lower() or "min" in ctx.lower()
           assert "Fase" in ctx or "fase" in ctx

       def test_should_nudge_false_before_80_percent(self):
           state = InterviewState(900)
           state.started_at = time.time() - 600  # 66% elapsed
           assert state.should_nudge == False

       def test_should_nudge_true_at_80_percent(self):
           state = InterviewState(900)
           state.started_at = time.time() - 720  # 80% elapsed
           assert state.should_nudge == True

       def test_should_nudge_fires_only_once(self):
           state = InterviewState(900)
           state.started_at = time.time() - 750  # > 80%
           assert state.should_nudge == True
           state.mark_nudged()
           assert state.should_nudge == False

       def test_should_force_close_false_before_95_percent(self):
           state = InterviewState(900)
           state.started_at = time.time() - 800  # ~89% elapsed
           assert state.should_force_close == False

       def test_should_force_close_true_at_95_percent(self):
           state = InterviewState(900)
           state.started_at = time.time() - 855  # 95% elapsed
           assert state.should_force_close == True

       def test_transition_to_valid_phase(self):
           state = InterviewState(900)
           state.transition_to('conversation')
           assert state.phase == 'conversation'
           state.transition_to('closing')
           assert state.phase == 'closing'

       def test_transition_to_invalid_phase_raises(self):
           state = InterviewState(900)
           with pytest.raises((ValueError, KeyError)):
               state.transition_to('invalid_phase')

       def test_topics_count_increment(self):
           state = InterviewState(900)
           assert state.topics_count == 0
           state.topics_count += 1
           assert state.topics_count == 1
   ```

5. Add `pytest` and `pytest-asyncio` to `agent/requirements.txt` as dev dependencies (or note in action that they need to be installed for testing):
   ```bash
   cd agent && pip install pytest pytest-asyncio
   ```
  </action>
  <verify>
    <automated>cd agent && python -m pytest tests/test_interview_state.py -v 2>&1 || echo "Tests will pass after Plan 02 Task 1 creates interview_state.py"</automated>
  </verify>
  <done>Python test infrastructure created: pytest.ini configures test discovery, conftest.py provides mock fixtures for Supabase and InterviewConfig, test_interview_state.py has 12 tests covering all timing guardrails (80% nudge, 95% force-close, nudge-once behavior, phase transitions). Tests will pass after Plan 02 creates the InterviewState class.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- test files only.

## STRIDE Threat Register

No threats -- test infrastructure only.
</threat_model>

<verification>
- All 4 frontend test stub files exist and Vitest discovers them
- agent/pytest.ini exists with testpaths = tests
- agent/tests/conftest.py exists with mock fixtures
- agent/tests/test_interview_state.py exists with 12 test methods
- `npx vitest run tests/api/ tests/components/ --reporter=verbose` passes (stubs)
</verification>

<success_criteria>
Test infrastructure ready for Phase 3. Frontend test stubs exist for token route (WEBR-01), transcript feed (WEBR-05), text fallback input (WEBR-07), and interview room (DASH-05). Python test infrastructure with pytest.ini, conftest.py, and InterviewState guardrail tests (WEBR-08). All test files are discoverable by their respective test runners.
</success_criteria>

<output>
After completion, create `.planning/phases/03-voice-interview/03-00-SUMMARY.md`
</output>
